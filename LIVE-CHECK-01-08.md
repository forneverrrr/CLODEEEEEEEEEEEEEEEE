# Проверка живого сайта — 1 августа 2026

**Метод:** каждая страница скачана с nordindsigt.dk и проверена скриптом
(JSON-LD, hreflang, canonical, RTL, парность тегов), не глазами.

---

## Итог: заливка полная и правильная

**32 новые страницы (8 статей × 4 языка) отдают HTTP 200 и проходят все
проверки. Ошибок — 0.**

| Проверка | Результат |
|---|---|
| JSON-LD: Article + BreadcrumbList, без FAQPage | ✅ на всех 32 |
| `author: Person`, `publisher.sameAs` | ✅ на всех 32 |
| Видимый FAQ вместо невидимого JSON-LD | ✅ на всех 32 |
| hreflang — 5 ссылок | ✅ на всех 32, **включая восьмую статью** |
| canonical корректный | ✅ на всех 32 |
| `dir="rtl"` на арабских | ✅ |
| Активный язык в переключателе | ✅ |
| Парность тегов | ✅ |

---

## Расхождение из прошлого разговора — разрешилось само

Я держал открытым вопрос: планировалось «14 статей на `/viden.html`», а по моим
расчётам должно было стать 16 (8 старых + 8 новых). **Проверил живую
`/viden.html` — там 16.** Все 8 новых плюс существовавшие 8 (включая три,
которых нет в репозитории: `ejerudgift`, `tilstandsrapport`, `asbesttag`).
Вопрос закрыт правильным числом.

## Восьмая статья переведена — не мной

`viden-soege-selv-eller-hjaelp` я оставил без переводов, потому что не успел их
сделать. На живом сайте она уже переведена на EN/UK/AR — проверил построчно:
это настоящий переведённый текст («Searching for a home yourself is free in
money…», «Коли шукати житло самому вже не має сенсу?», арабский H1), не зеркало
датского и не заглушка. Значит перевод сделан либо тобой, либо ещё одной сессией.
hreflang=5 у неё на сайте — правильно, моя проверка ошибочно ожидала 2.

## Пять уже существовавших статей — обновление тоже прошло

`viden-andelsbolig`, `viden-bydel`, `viden-case-to-huse-samme-pris`,
`viden-elrapport`, `viden-skjulte-udgifter` — на всех пяти живых страницах есть
и добавленные `.note`-блоки с цифрами, и видимый FAQ, и `sameAs`/`Person`
не потеряны.

## Инфраструктура

- **`pricing.md`** — HTTP 200, отдаётся как настоящий markdown, не HTML-обёртка.
- **`llms.txt`** — HTTP 200, **это реальный файл, не SPA-фолбэк**. Внутри
  услуги, цены, и теперь **все 8 новых статей включены** в список — значит его
  тоже обновили при заливке.
- **`robots.txt`** — по-прежнему `Allow: /`, AI-боты не заблокированы.
- Проверил формат hreflang на посторонней старой статье (`viden-tilstandsrapport`)
  для сверки — совпадает с нашим форматом 1:1.

---

## Что осталось открытым

- **Правило 5 лет резидентства / ApS по Civilstyrelsen** всё ещё не
  верифицировано — источник вторичный (Wise). Абзац про это нигде не публиковался,
  так и остаётся заблокированным до проверки.
- Тексты новых статей на сайте не сверялись построчно с репозиторием — проверка
  была по структуре (JSON-LD, hreflang, теги), не по слову в слово. Если нужна
  построчная сверка контента — отдельный проход.

**Вывод: заливка сделана полностью и правильно, ревью прошло вживую без единой
ошибки.** Следующий блок плана — пробные клипы Veo и первый рилс — можно
начинать без оглядки на сайт.
